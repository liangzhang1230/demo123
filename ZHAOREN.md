# 销冠招人器 · `zhaoren.html`

《销冠算盘》2 号板块的完整实现。**单个 HTML 文件，双击即用**——无后端、无网络请求、无第三方 CDN、无构建步骤，数据经 `localStorage`（前缀 `skab_zhaoren_`）落本机。严格遵《0号·全局公约》技术栈冻结（§8）与《2号·招人器规格 v3.2》。

## 怎么用

1. 双击 `zhaoren.html`（或拖进浏览器）。
2. 左栏九屏：首页 → 七步输入 → 结果 → 招聘包 → 漏斗水温 → 批次入职 → 效度实验室 → 数据 → 系数自检。
3. 首页点「载入示例数据」即可看旗舰结论：**天真 8 人 vs 七步链 20 人 · 高估率 60% · 最晚开招 2025-11-17（晚 238 天）**。
4. 「系数自检」屏看徽章：**24/24 绿 = 收货**。徽章跑的是与产品逐字同源的引擎。

侧栏三枚开关：`full/free 版`（授权粒度降级演示）、`演示钟/真实钟`（时钟注入，公约 C-14）、`主题`（浅色 / 深色 / 跟随系统）。

## 一次成型的做法：验证驱动，不是"写完就交"

规格自己定义了交付标准——**三绿方交付**（件七 24 条对拍）。所以先锁引擎，再包界面：

```bash
node zhaoren-verify/tests.cjs     # 24 条对拍 + 闸①③ 激活 = 26 绿 / 0 红
```

`zhaoren-verify/engine.cjs` 是纯函数引擎的**唯一事实源**；它被逐字内联进 `zhaoren.html` 的 `/* ENGINE:BEGIN */ … /* ENGINE:END */` 区块。同一套断言又在浏览器内跑一遍（系数自检屏），并用真实 Chromium 无头驱动核过：24/24 绿、零 JS 报错、九屏全渲染。

对拍口径全部按规格逐格复算并通过，含 C-04 头部和 `newHireYearRate`、C-17 `kEff` 接线、F11 皮尔逊 r（+0.38 / −0.09 / −0.11，内置模拟集反解锁定）、F15 价签 48.17 万 / 内推奖 ¥4,800 / QCS 88 vs 61 等。

## 设计原则：双极简

- **系统极简**：一个文件、零依赖、零网络、零构建、`localStorage`、双击即用。系统最小到"能永久离线自持"。启动做配额自检（占用估算 + 写入探针），距上次导出 > 14 天顶部黄条催备份（A-24）。
- **用户极简**：一屏一结论、渐进披露（十二闸折成一行，展开 = 现象→机制→📎出处→建议 + ActionCard）、`null`→"—" 从不假装为 0、每条建议带出处行、每个数字经得起评论区专家较真。

外形借鉴全球头部：Linear/Superhuman 的克制与"一屏一事"、Stripe 的金融级数字排版（等宽数字、发丝级描边而非重投影）、Apple/Geist 的类型阶梯与"一屏一主动作"。

## 红线（已内建）

数据不出本机（无 `fetch`/上传）· 销售端零像素利润口径 · 系统永不自动执行人事动作（一切走 ActionCard 由老板点击）· 黑暗三角明细转正即销毁（仅留数值供匿名统计，Z-D1）· 授权署名可见 + 到期只降级不锁数据（授-1/授-2）· 边界声明五板块同句：『这个系统不会让一个卖不动的产品卖动。』
