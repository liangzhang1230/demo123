/** /manager · 主管刘敏 · 同板裁剪本部门＋摘除购买位与投产参照 */
import { ManagementBoard } from '../../components/ManagementBoard';

export default function ManagerPage() {
  return <ManagementBoard role="manager" />;
}
